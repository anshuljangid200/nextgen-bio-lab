PORT=5000

# Micrylis MongoDB (add when ready — not the old website database)
# MONGODB_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/micrylis

# Admin email on login / register (Resend — same as contact form)
RESEND_API_KEY=re_your_resend_key
ADMIN_EMAIL=contact@micrylisbiotech.com
RESEND_FROM=onboarding@resend.dev
